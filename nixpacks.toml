[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["echo 'No install needed - using standard library only'"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "python3 server_standalone.py"

